apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitpin-app.fullname" . }}
  labels:
    {{- include "bitpin-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bitpin-app.selectorLabels" . | nindent 4 }}
